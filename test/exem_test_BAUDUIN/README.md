# 📦 Dossier d'exemple d'examen — `exem_test_BAUDUIN`

Ce dossier est un **exemple technique** utilisé pour s'entraîner à :

- organiser des fichiers dans une structure simple,
- créer une archive `.tar` à partir d'un dossier,
- transférer cette archive depuis la machine virtuelle (VM) vers une machine locale,
- préparer un livrable conforme aux attentes des examens DataScientest.

Il s'agit d'un dossier d'entraînement destiné à la manipulation de fichiers et à la création de livrables d'examen.

---

## 📁 Contenu du dossier

> **⚠️ L’archive `.tar` n’est pas dans ce dossier.**  
> Elle se trouve au niveau parent (`test/`), car elle a été générée *à partir* de ce répertoire.

Voici exclusivement le contenu réel du dossier :

exem_test_BAUDUIN/

├── note.txt # fichier d’exemple

└── reponse.txt # fichier d’exemple


---

## 📦 L’archive générée (`exem_test_BAUDUIN.tar`)

Une archive nommée :  exem_test_BAUDUIN.tar


a été créée **à partir de ce dossier**, puis placée au niveau parent.  
Elle sert d’exemple pour apprendre à :

- compresser un dossier dans la VM,
- récupérer un fichier `.tar` en local,
- soumettre ce type d'archive lors d’un examen ou d’un rendu.

Cette archive peut contenir **n’importe quel type de fichier**, l’objectif étant uniquement de pratiquer la création et la récupération d’un livrable.

---

## 🎯 Objectif pédagogique

Ce dossier permet de s'entraîner à manipuler :

- la création d'archives `.tar`,
- la préparation d’un livrable d’examen,
- le transfert VM → machine locale,
- la compréhension du format attendu lors des évaluations.

Il pourra servir de modèle pour tous les futurs examens nécessitant un dossier compressé.


